import React from 'react';
import { Select, ControlLabel, FormControl, HelpBlock } from 'react-bootstrap';

const SelectBoxForm = ({ value, onChange, name, classes, children, ...props }) => {
  return (
    <FormControl className={classes.formControl}>

    </FormControl>
  )
};

export default SelectBoxForm;
